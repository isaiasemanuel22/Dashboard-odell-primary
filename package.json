{
  "name": "odell-dashboard",
  "private": true,
  "scripts": {
    "frontend:build": "npm run build:prod --prefix frontend",
    "frontend:deploy": "npm run frontend:build && npx netlify-cli deploy --prod --dir=frontend/dist/frontend/browser",
    "frontend:deploy:preview": "npm run frontend:build && npx netlify-cli deploy --dir=frontend/dist/frontend/browser"
  }
}
